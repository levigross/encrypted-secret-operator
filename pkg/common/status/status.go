package status

// KeyStatus is what we use to tell our user about the status of the keys that they have submitted
type KeyStatus struct {
}

// SecretStatus is the status we return when we create a secret
type SecretStatus struct {
}
